Project: SingletonPatternExample

This project demonstrates the Singleton Design Pattern in Java.
It ensures only one instance of Logger is created during the application lifecycle.

By running LoggerTest.java we can see the singleton in action.
